---
name: academic-word
description: 创建、修改和排版学术Word文档，适用于含MathType公式、原生参考文献自动编号、题注和交叉引用的中文学位论文、开题报告及学术稿件。用于保留原稿的DOCX版本化编辑及Word COM核验；用户明确要求只润色文字、暂不修改Word时，不执行文档修改。
---

# Academic-word：学术Word文档处理

以用户最新修改的文档为底稿，保留科学含义、公式、图件和有意设置的排版。交付可直接使用的Word文档，而不是仅在外观上类似Word文档的文字。

## 根据任务选择处理方式

- **润色或扩写：**先确定具体章节及相关内容，再动笔。改善表述，但不虚构实验、性能指标、实测结果、参考文献或研究内容。研究内容发生变化时，检查相关研究目标、技术路线、实验步骤、创新点和可行性，只修改本次请求涉及的内容。
- **编号、引用或域修复：**阅读[Word编号、交叉引用与脚本用法](references/word-mechanics.md)。使用Word原生功能，保留MathType及文献管理软件的域。这里的“域”是Word用于自动生成编号、引用等内容的结构，不是普通文本。
- **中文学术排版：**用户要求使用已保存的格式规范，或文档已经采用这些规范时，阅读[中文学术排版偏好](references/chinese-format-profile.md)。这些是可调整的个人偏好，并非所有学术文档都必须遵循的统一规则。
- **使用配套脚本：**修改前阅读`word-mechanics.md`中的脚本用法。脚本只处理明确支持的情况，不应为了套用脚本而替换文档中原本有效的结构。

## 保留用户实际修改的底稿

1. 确认指定文件、修改时间和哈希值（用于判断文件内容是否变化的“指纹”）。“接收修改”表示以用户最新保存的内容为准，**不代表允许接受全部Word修订记录**。修订记录需要单独检查。若原稿可能还有未保存的修改，先请用户保存或确认，不得擅自保存其正在使用的Word窗口。
2. 制作与原文件逐字节一致的备份或工作副本。默认不覆盖原稿，另存为带新版本号的文件，交付前再次核对原稿哈希值。不要因为文件名熟悉，就误用较早生成的版本。
3. 统计段落文字、域和书签、编号、图件、MathType嵌入对象，以及分节和表格尺寸。只修改要求处理的范围。需要保留公式对象和域时，优先定点修改OOXML（DOCX内部的XML结构）或Word COM范围对象，不要用`python-docx`重建整份文档。
4. 如果修订记录、Zotero/EndNote管理的域、含义不明的引用编号或重叠书签使修改无法可靠进行，先检查其所属结构。不要通过转成普通文本、解除锁定或随意重编编号来强行处理。

## 忠实于资料，保持科学表述准确

- 区分设计、计算、仿真、芯片实测和计划开展的实验。没有充分证据时，不能仅凭透射谱就断言没有高阶模式。
- 区分几何尺寸与物理量。宽度和厚度、单位、实测和仿真标注或资料间差异会影响科学含义时，应先澄清。
- 用户要求使用本机Zotero文献时，以只读方式检索本地文献库，并查看相关全文。只引用能够直接支持相应论述的文献。改变引用编号时，必须保持所指文献不变；完成编号转换不等于完成文献内容核验。
- 加入理论推导时，核对原文公式、变量定义、正负号约定、近似条件和量纲。区分文献已有结果与新增推论，不加入后文不用的符号或整套无关的测量方法。
- 用户要求把“具体研究流程如图X所示。”移到每一点开头时，应将包含交叉引用域的原句移到该点**标题之后、正文段落的开头**。不要把域替换成普通文本，也不要改变图件顺序。

## Word文档的具体处理要求

- 安装了Microsoft Word时，使用独立的Word COM实例进行受控更新和渲染。COM是通过程序调用Word的接口。禁用宏自动运行，以只读方式打开输入文件，另存新文件，只关闭本次任务创建的文档和实例。
- 参考文献使用编号格式为`[%1]`的Word原生编号列表；题注使用`SEQ`域；正文引用使用`REF`域。仅把数字设成上标，或给数字添加超链接，都不等于建立交叉引用。
- 现有MathType公式必须保留为可编辑的OLE嵌入对象。配套脚本能够保留这些对象，但**不能将任意公式自动转换为MathType公式**。OMML是Word原生可编辑公式，并非MathType。用户明确要求转换时，应先在临时副本上验证已安装MathType的API或插件流程；无法完成时，说明限制。
- 只更新本次需要处理的域。全选后按F9，或统一调用`Fields.Update()`，不能安全地处理Word、MathType和文献管理软件的所有更新行为。不要批量解除所有域的锁定。
- 表格宽窄不一或内容被遮挡时，检查所在节的可用正文宽度、表格宽度与缩进、单元格边距、行高规则，以及跨页断行和段落保持设置。只修复有关尺寸与设置，不要删除边框或盲目统一所有表格。渲染后检查跨页续表和下边框。
- 使用原始图件，或提取PPT中的单独对象、局部图形。用户未要求时，不插入整页PPT截图。不推断图中未给出的数据；用户要求隐去具体扫参值或延时量时，流程图标签也必须遵守。

## 核验与交付

- **在Word保存后**检查文档结构：原稿未变；文字变化仅限预期范围；原生编号及域的显示结果正确；书签可以定位；没有域错误提示；图片和嵌入对象的哈希值及对应数量未发生意外变化。除了对象数量，还要检查原有域代码。
- 渲染最终DOCX并逐页查看。优先使用环境提供的文档渲染工具；不可用时，通过Word导出PDF，再用`render_pdf.py`生成页面图片。以足够清晰的尺寸检查修改页的参考文献缩进、换行、题注、公式、表格边界和分页。缩略图总览只能辅助定位，不能证明公式清晰可读。
- 页数相同、图片和嵌入文件未变，都不能代替视觉检查。如果无法渲染，应说明未完成排版核验，不得声称已检查。
- 交付最终版本，并简要说明修改内容。除非用户需要，否则不交付核验用PDF、页面图片、日志和工作副本。准确说明实际测试了什么；不能仅凭静态XML检查，就声称完成了界面编辑或文献管理插件的更新。

## 配套资源

- `scripts/docx_ops.py`：检查和审计DOCX；把手打的`[n]`转换为原生编号列表及REF交叉引用；为已有的简单SEQ题注建立仅含标签和编号的引用；按需规范格式；移动句子时保留域。
- `scripts/word_finalize.ps1`：通过Word COM选择性更新域、另存新文件、导出PDF，并实时核对编号和域的显示结果。
- `scripts/render_pdf.py`：把已有PDF渲染成逐页图片和缩略图总览。
- `scripts/test_docx_ops.py`：离线回归测试，检查关键文档结构是否保留，以及不支持的输入是否被安全拒绝。
- `scripts/test_word_fields.ps1`：在临时Word文档中测试方括号编号、引用区间端点的纯数字显示，以及编号变化后的交叉引用更新。

运行依赖：Python及`lxml`；测试还需`python-docx`；渲染还需`pdf2image`、Pillow和Poppler。Word COM需要Windows及已安装的Microsoft Word。先查找当前环境可用的内置运行时，不硬编码其他用户的本机路径，也不无故安装依赖。
